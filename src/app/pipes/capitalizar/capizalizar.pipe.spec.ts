import { CapizalizarPipe } from './capizalizar.pipe';

describe('CapizalizarPipe', () => {
  it('create an instance', () => {
    const pipe = new CapizalizarPipe();
    expect(pipe).toBeTruthy();
  });
});
