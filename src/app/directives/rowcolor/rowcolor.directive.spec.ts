import { RowcolorDirective } from './rowcolor.directive';

describe('RowcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new RowcolorDirective();
    expect(directive).toBeTruthy();
  });
});
