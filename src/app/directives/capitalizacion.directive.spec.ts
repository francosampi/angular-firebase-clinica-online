import { CapitalizacionDirective } from './capitalizacion.directive';

describe('CapitalizacionDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizacionDirective();
    expect(directive).toBeTruthy();
  });
});
